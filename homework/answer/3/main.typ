#import "@preview/tyniverse:0.2.3": homework
#import "@preview/zebraw:0.6.1": *
// #import "@preview/subpar:0.2.2"
// #import "@preview/fletcher:0.5.8" as fletcher: diagram, edge, node
// #import "@preview/pinit:0.2.2": *

#set page("a4")
#show: homework.template.with(
    course: "数据库系统",
    number: 2,
    student-infos: ((name: "彭靖轩", id: "202400130242"),),
)
#set enum(numbering: n => emph(strong(numbering("a.", n))))
#show: zebraw.with(lang: false)
#set smartquote(quotes: "\"\"")
#let (rr, ll) = (sym.gt.eq.slant, sym.lt.eq.slant)

#homework.simple-question[_11.1_]
1. 存储开销大:每个索引都要占额外磁盘空间.
2. 更新开销高:插入/删除/更新元组时,相关索引都要维护,写操作会明显变慢.
3. 优化器收益有限:大量低选择性或很少被查询使用的索引,通常不会被选中,反而增加优化与维护成本.
4. 组合索引数量爆炸:属性组合很多时,索引数会指数增长,实际不可维护.

#homework.simple-question[_11.2_]
一般不可行(或意义不大).聚集索引要求数据记录在物理上按该搜索码顺序存放,而一份数据在同一时刻通常只能按一种主顺序组织,因此一个关系通常只能有一个聚集索引;其他索引一般应为非聚集索引.

#homework.simple-question[_11.9_]
通常按"工作负载驱动"来选:
1. 先统计高频且代价高的查询(选择、连接、排序、分组).
2. 优先给过滤条件和连接条件中选择性高的属性建索引.
3. 对多条件查询按最常见谓词顺序设计复合索引(遵循最左前缀原则),并尽量覆盖查询以减少回表.
4. 若查询经常按某属性范围扫描或排序,考虑在该属性上建$B^+$树索引;低基数且读多写少场景可考虑位图索引.
5. 兼顾更新代价与空间:删除很少使用或重复功能的索引,通过实际执行计划与性能测试迭代调整.

#homework.simple-question[_11.10_]
设图中记录顺序为1到12(从上到下).在_salary_上的4个区间位图为:

1. $B_(<50000)=001000000000$
2. $B_([50000,60000))=000000000000$
3. $B_([60000,70000))=100010010000$
4. $B_(rr 70000)=010101101111$
先构造系别位图(按同一记录顺序):
$ B_{"Finance"}=010000001000 $
由于工资位图只分到$rr 70000$,先用它做候选筛选:
$ B_1 = B_{"Finance"} and B_{rr 70000} = 010000001000 $

即第2和第9条.再回表检查精确条件$"salary" rr 80000$:

1. 第2条:Wu, Finance, 90000
2. 第9条:Singh, Finance, 80000
所以最终位图:

$ B_{"final"}=010000001000 $
最终结果:Wu, Singh.

#homework.simple-question[_11.11_]
1. 数据文件不是按该搜索码顺序存储(即非聚集索引),稀疏索引无法有效定位到每条记录.
2. 查询需要高选择性等值查找,希望尽量减少顺序扫描数据块.
3. 记录是变长、页分裂/重组频繁,仅按"每块一个索引项"不够稳定或不够精确.
4. 可以接受更高索引空间和维护代价,以换取更快检索.

#homework.simple-question[_11.15_]
1. 对$10<A<50$:可先定位到$A>10$的第一个索引项(代价约$h$),再顺序扫描到$A rr 50$为止并取记录.最坏代价$h+n_1$.
2. 对$10<A<50 and 5<B<10$:由于索引按$(A,B)$排序,先按上一步扫描得到$A$范围内候选,再检查$B$条件.最坏仍需扫描这$n_1$个候选,代价约$h+n_1$;其中真正满足条件的是$n_2$条.
3. 该索引对第二个查询高效当且仅当过滤效果明显,即$n_2 ll n_1$.若$n_2 approx n_1$,则额外$B$条件几乎不减少扫描,效率提升有限.
