#import "dependency.typ": *
#import "template.typ": *

#show: report.with(
    institute: "计算机科学与技术",
    course: "数据库系统",
    student-id: "202400130242",
    student-name: "彭靖轩",
    date: datetime.today(),
    lab-title: "SQL实验1-8总结",
    class: "24智能",
    email: link("mailto:arshtyi@foxmail.com"),
)
#show figure.where(kind: "image"): it => {
    set image(width: 67%)
    it
}

#exp-block([
    = 实验目的
    + 掌握 Oracle SQL 的核心操作流程,包括 `CREATE TABLE`、`INSERT`、`UPDATE`、`DELETE`、`CREATE VIEW`、`GROUP BY/HAVING`、子查询与窗口函数等.
    + 能基于给定业务场景(银行、储户、存款)完成数据查询、数据清洗、统计分析与结果落.
])
#exp-block([
    = 实验软件和硬件环境
    + Windows11.
    + Oracle.
])
#exp-block([
    = 实验原理与方法
    == Lab1-数据定义与装载
    + 通过 `CREATE TABLE` 定义表结构，使用 `NOT NULL`、数值/日期类型约束字段语义.
    + 使用 `INSERT INTO ... VALUES` 完成基础样例数据装载，建立后续实验所需最小数据集.

    == Lab2&6-条件查询与视图封装
    + 采用连接、嵌套子查询、`EXISTS/NOT EXISTS`、集合差、双重否定等方式表达复杂条件.
    + 将查询封装为视图,形成可复用中间结果,降低重复编写 SQL 的成本.

    == Lab3&4-数据清洗与修正
    + 利用 `REGEXP_LIKE`、`REGEXP_REPLACE`、`TO_CHAR/SUBSTR` 校验和修复身份证、姓名、性别、年龄等字段.
    + 采用"复制原表 -> 条件删除/更新"的方式实现可回溯的数据清洗流程.

    == Lab5,6&7-统计汇总与分析
    + 使用 `COUNT/SUM/AVG`、`ROLLUP`、分组统计、分段统计、代际统计、TopN 排名(`RANK`)完成多维分析.
    + 使用 `WITH` 公共表达式构建分步计算链路,提高 SQL 可读性与可维护性.

    == Lab8-事务与权限验证
    + 通过主账户与临时账户协作,对 `SELECT/INSERT/UPDATE` 授权对象开展事务实验.
    + 对比不同时刻 `COMMIT/ROLLBACK` 前后的可见性,验证事务持久化与回滚机制.
])
#exp-block([
    = 实验步骤
    + 实验1:创建 `test1_bank`、`test1_deptor`、`test1_deposit` 三张表并插入样例记录,验证建表和数据写入流程.
    + 实验2:完成 10 组查询并封装为视图,重点练习多条件筛选、父子关系查询、跨城市存款条件、全称量条件(双重 `NOT EXISTS`).
    + 实验3:基于 `bk.deptor3` 构建副本表,逐题按规则删除不合格记录,完成身份证、生日、性别、姓名编码、年龄一致性等数据清洗.
    + 实验4:在副本表上执行字段标准化与批量更新，补充统计列(总笔数、总金额、平均金额、利息)并完成 Top20 结果筛选.
    + 实验5:进行聚合统计与多维分析,包括按网点、按城市、按代际、按金额区间、按时间维度汇总,并构建相应结果表/视图.
    + 实验6:进行进阶查询训练,覆盖姓名字符频次统计、条件聚合筛选、每银行最大存款及并列人数、女性全覆盖银行等题型.
    + 实验7:以"结果入表"为核心,先建空表与唯一索引,再插入目标集合,并扩展到包含统计字段的结果集构造.
    + 实验8:在跨用户授权表上执行更新、提交、回滚、查询等操作,记录主/从账户观察结果并归纳事务行为.
    // + 对照 `answer/answer.md` 核验关键题目逻辑,修正个别写法差异,确保结果语义与题意一致.
])
#exp-block([
    = 结论分析与体会
    + 对同一题目，常存在多种等价写法(如连接 vs `EXISTS`、子查询 vs `WITH`).
    + 视图与中间表能显著提升复用效率.
    + 数据清洗类题目中,"先复制再改动"是更安全的实践,可避免直接破坏原始数据.
    + 聚合统计的关键在于明确分组粒度;分组字段选错会导致统计口径偏差.
    + 事务实验说明:`COMMIT` 后修改对其他会话可见且可持久,`ROLLBACK` 仅回退本事务中尚未提交的改动.
])
#exp-block([
    = 实验过程中遇到和出现的问题及如何解决和处理
    + 问题:身份证与生日校验时，日期格式转换容易写错.\
      处理:统一采用 `TO_CHAR(birthdate, 'YYYYMMDD')` 与 `SUBSTR(pid, 7, 8)` 对齐比较.
    + 问题:复杂条件下 `IN`、`EXISTS` 结果不一致,出现漏数.\
      处理:按"是否需要相关子查询"判断写法,并用小样本先验证逻辑再全量执行.
    + 问题:统计题中 `NULL` 参与运算造成结果为空.\
      处理:在聚合和计算列中使用 `NVL`/`NULLIF` 防止空值传播或除零错误.
])
